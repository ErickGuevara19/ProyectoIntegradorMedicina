import { DoctorListPipe } from './doctor-list.pipe';

describe('DoctorListPipe', () => {
  it('create an instance', () => {
    const pipe = new DoctorListPipe();
    expect(pipe).toBeTruthy();
  });
});
