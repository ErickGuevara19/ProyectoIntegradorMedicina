import { MedicalAsignationPipe } from './medical-asignation.pipe';

describe('MedicalAsignationPipe', () => {
  it('create an instance', () => {
    const pipe = new MedicalAsignationPipe();
    expect(pipe).toBeTruthy();
  });
});
